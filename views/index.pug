extends layout

block hero-unit
  #carousel.carousel.slide(data-ride='carousel')
    .carousel-inner
      .carousel-item.active
        img.w-100(src='/images/hero1.jpg', alt='First slide')
      .carousel-item
        img.w-100(src='/images/hero2.jpg', alt='Second slide')
      .carousel-item
        img.w-100(src='/images/hero3.jpg', alt='Third slide')
      .carousel-item
        img.w-100(src='/images/hero4.jpg', alt='Third slide')

block hero-unit2
  .container
    .card.text-center.card1
      .card-body
        h1.card-title Find your next experience
        form
          .form-row
            .col
              input(type='text', class='form-control', placeholder='Search events or categories')
            .col      
              input(type='text', class='form-control', placeholder='City or location')
            select.custom-select#select
              option.selected All Dates
              option(value='1') Today
              option(value='2') Tomorrow
              option(value='3') This Week
              option(value='4') This Weekend
              option(value='5') Next Week
              option(value='6') Next Month
            button.btn.btn1(type='submit') SEARCH

block content
  .container
    .row
      .col-md-6
        h3 서비스 목적
        p 본 서비스는 웹프로그래밍 수업을 위해서 sample로 만들어보는 것입니다. 
      .col-md-6
        h3 주요 기능
        ul
          li 질문글 등록/보기/변경/삭제
          li 답변글 등록/삭제
          li 사용자 관리/인증
          li API

